# AGENTS.md

## 文档规范

本节适用于创建、修改、移动或删除 `docs/**` 中的任何文档。这里是 Agents 必须遵守的文档分类、写作、同步和校验规范；`docs/README.md` 仅供人类阅读，不作为 Agent 指令来源。

### 1. 目录与分类

- `docs/agents/` 保存能够直接驱动实现、排错、审查和验证的详细事实、约束与验收基线。
- `docs/humans/` 保存供人类快速理解、决策和使用的精简文档。
- `docs/README.md` 是人类文档入口和索引，也是 `docs/` 根目录唯一允许的普通文档。
- 除 README 外，每份文档必须且只能放入一个主要受众目录。
- 以主要消费者决定分类，不以文档作者身份决定分类。
- 同一主题只有确实需要不同信息密度时才创建双版本，禁止把完整内容复制到两个目录。
- 同一主题存在双版本时，`agents/` 中的详细文档默认是实施事实基线；`humans/` 中的文档是精简摘要并必须链接详细文档。
