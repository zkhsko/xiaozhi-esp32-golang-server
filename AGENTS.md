# AGENTS.md

## 文档规范

本节适用于创建、修改、移动或删除 `docs/**` 中的任何文档。这里是 Agents 必须遵守的文档分类、写作、同步和校验规范；`docs/README.md` 仅供人类阅读，不作为 Agent 指令来源。

### 1. 目录与分类

- `docs/agents/` 保存能够直接驱动实现、排错、审查和验证的详细事实、约束与验收基线。
- `docs/humans/` 保存供人类快速理解、决策和使用的精简文档。
- `docs/README.md` 是人类文档入口和索引，也是 `docs/` 根目录唯一允许的普通文档。
- 除 README 外，每份文档必须且只能放入一个主要受众目录。
- 以主要消费者决定分类，不以文档作者身份决定分类。
- 同一主题只有确实需要不同信息密度时才创建双版本，禁止把完整内容复制到两个目录。
- 同一主题存在双版本时，`agents/` 中的详细文档默认是实施事实基线；`humans/` 中的文档是精简摘要并必须链接详细文档。
- 文档开头必须注明目标读者和状态；存在对应版本或上游事实来源时必须提供相对链接。

### 2. Agents 文档写法

`docs/agents/` 中的文档必须让 Agent 在没有历史对话的情况下准确恢复上下文并执行任务：

- 明确写出目的、适用范围、当前状态、事实来源和完成定义。
- 将已确认事实、人工决策、硬约束、非目标和未决事项分开表达。
- 使用明确、可验证的措辞，禁止使用“适当”“视情况”“大概支持”等无法执行的描述。
- 关键协议必须写明字段、时序、状态、边界、失败行为和兼容口径。
- 关键实现必须写明目录、依赖方向、数据模型、并发所有权、资源上限和安全边界。
- 验收要求必须映射到具体检查、测试、命令或外部证据，禁止用笼统的“测试通过”代替。
- 明确结论来自代码、配置、官方文档还是人工决策；存在冲突时写明最终事实来源。
- 使用稳定标题、表格、列表、代码块、精确路径和精确术语，便于扫描与检索。
- 不记录思维过程、无效尝试、会话寒暄或无法复现的临时上下文。
- 不创建没有明确生产入口、读者或维护责任的文档。

### 3. Agents 编写人类文档

Agent 创建或修改 `docs/humans/` 文档时，必须主动降低信息密度：

- 先给结论，只保留目标、范围、关键决策、状态、风险、限制和下一步。
- 使用短段落、表格和扁平列表，保证读者能够快速获取关键信息。
- 删除内部 package、完整依赖清单、逐字段协议、底层并发细节和完整测试矩阵。
- 实现细节不影响人类决策或使用时不展开，链接到对应 Agents 详细文档。
- 不重复结论，不写背景性套话，不把详细文档机械压缩或逐段改写。
- 风险、限制、未验证事项和临时 Mock 不得因精简而被隐藏。
- 人类文档不得自行增加或改变详细事实基线中的实施承诺。

判断标准：删除某项细节后，如果不影响目标理解、方案选择、正确使用或风险判断，就必须删除该细节。

### 5. 同步与维护

- 修改产品范围、关键决策、安全边界、外部接口或验收结论时，必须在同一次变更中检查人类摘要是否需要同步。
- 只修改内部实现细节且不影响人类理解时，只更新 Agents 文档，不向人类文档搬运噪音。
- 人类文档与详细事实基线发生冲突时，先修正冲突，再交付变更。
- 删除文档时同步删除只为它存在的索引和引用；重命名后不得保留旧路径兼容文件。
